experiment_name: smoke
experiments:
  - soic_leakage

hf_token_env: HF_TOKEN
num_seeds: 1
seed_list: [11]

resources:
  batch_size: 2
  max_prompts: 5
  attacker_epochs: 1

datasets:
  secret_prompts:
    repo: synthetic://secret_prompts
  benign_prompts:
    repo: synthetic://benign_prompts

models:
  # Use very small models for CI / smoke testing to avoid OOMs
  base:
    repo: sshleifer/tiny-gpt2
  hyperion:
    repo: sshleifer/tiny-gpt2
